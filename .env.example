# Copy this file to .env and adjust it for your deployment.
# .env is gitignored — set this once per host, it won't be committed
# or overwritten by future pulls.

# Hostname or IP of the machine running docker compose. Used to build
# the S3 endpoint URLs the go-backend hands out to clients.
SERVER_HOST=nas.local
